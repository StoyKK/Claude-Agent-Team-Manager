---
phase: 01-infrastructure
plan: 04
type: execute
wave: 2
depends_on: [03]
files_modified:
  - src/services/remote-sync.ts
  - src/store/tree-store.ts
  - src/store/ui-store.ts
  - src/App.tsx
  - src/components/settings/SettingsPanel.tsx
  - src/components/tree/OrgNode.tsx
autonomous: true
requirements: [LOG-02]

must_haves:
  truths:
    - "No console.log, console.warn, or console.error calls remain in the source code (outside of logger.ts itself)"
    - "All 55 former console calls now use logger.trace/debug/info/warn/error with a context string"
    - "Log messages preserve the original information content"
  artifacts:
    - path: "src/services/remote-sync.ts"
      provides: "35 console calls replaced with logger calls"
      contains: "logger.warn"
    - path: "src/store/tree-store.ts"
      provides: "11 console calls replaced with logger calls"
      contains: "logger.info"
    - path: "src/store/ui-store.ts"
      provides: "4 console calls replaced with logger calls"
      contains: "logger.warn"
    - path: "src/App.tsx"
      provides: "2 console calls replaced with logger calls"
      contains: "logger.info"
    - path: "src/components/settings/SettingsPanel.tsx"
      provides: "2 console calls replaced with logger calls"
      contains: "logger.warn"
    - path: "src/components/tree/OrgNode.tsx"
      provides: "1 console call replaced with logger call"
      contains: "logger.warn"
  key_links:
    - from: "src/services/remote-sync.ts"
      to: "src/services/logger.ts"
      via: "import { logger }"
      pattern: "import.*logger.*from.*logger"
    - from: "src/store/tree-store.ts"
      to: "src/services/logger.ts"
      via: "import { logger }"
      pattern: "import.*logger.*from.*logger"
---

<objective>
Replace all 55 console.log/warn/error calls across 6 source files with structured logger calls.

Purpose: Structured logging enables filtering by level and context, routes to the Tauri log sink in production, and follows the `[ATM] [LEVEL] [context] message` format (D-04). This eliminates raw console output and makes debugging significantly easier.

Output: Zero console.log/warn/error calls remaining in application source (only logger.ts itself uses console as a fallback).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@.planning/phases/01-infrastructure/01-03-SUMMARY.md

<interfaces>
From src/services/logger.ts (created in Plan 03):
```typescript
export const logger = {
  trace: (context: string, message: string) => log("trace", context, message),
  debug: (context: string, message: string) => log("debug", context, message),
  info:  (context: string, message: string) => log("info",  context, message),
  warn:  (context: string, message: string) => log("warn",  context, message),
  error: (context: string, message: string) => log("error", context, message),
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace console calls in remote-sync.ts and store files</name>
  <files>src/services/remote-sync.ts, src/store/tree-store.ts, src/store/ui-store.ts</files>
  <read_first>
    - src/services/remote-sync.ts
    - src/store/tree-store.ts
    - src/store/ui-store.ts
    - src/services/logger.ts (to confirm the logger API)
  </read_first>
  <action>
Add `import { logger } from "@/services/logger";` at the top of each file.

**remote-sync.ts (35 calls):**
Replace each console call using these context strings and levels:

| Pattern | Replacement |
|---------|-------------|
| `console.warn("[CryptoSession] ...")` | `logger.warn("CryptoSession", "...")` |
| `console.warn("[RemoteSync] Failed to broadcast:", err)` | `logger.warn("RemoteSync", "Failed to broadcast: " + String(err))` |
| `console.warn("[RemoteSync] Failed to send via relay:", err)` | `logger.warn("RemoteSync", "Failed to send via relay: " + String(err))` |
| `console.warn("[RemoteSync] Encryption failed:", err)` | `logger.warn("RemoteSync", "Encryption failed: " + String(err))` |
| `console.log("[RemoteSync] Server started on port", event.payload?.port)` | `logger.info("RemoteSync", "Server started on port " + event.payload?.port)` |
| `console.warn("[RemoteSync] Reconnect handler error:", err)` | `logger.warn("RemoteSync", "Reconnect handler error: " + String(err))` |
| `console.log("[RemoteSync] Server stopped")` | `logger.info("RemoteSync", "Server stopped")` |
| `console.warn("[RemoteSync] Push handler error:", err)` | `logger.warn("RemoteSync", "Push handler error: " + String(err))` |
| `console.log("[RemoteSync] Relay room created:", event.payload.room_code)` | `logger.info("RemoteSync", "Relay room created: " + event.payload.room_code)` |
| `console.log("[RemoteSync] Peer joined relay room...")` | `logger.info("RemoteSync", "Peer joined relay room - awaiting PIN auth")` |
| `console.log("[RemoteSync] Peer disconnected from relay")` | `logger.info("RemoteSync", "Peer disconnected from relay")` |
| `console.log("[RemoteSync] Disconnected from relay")` | `logger.info("RemoteSync", "Disconnected from relay")` |
| `console.warn("[RemoteSync] Failed to process relay message:", err)` | `logger.warn("RemoteSync", "Failed to process relay message: " + String(err))` |
| `console.warn("[RemoteSync] Failed to connect to relay:", err)` | `logger.warn("RemoteSync", "Failed to connect to relay: " + String(err))` |
| `console.log("[RemoteSync] Desktop registered...")` | `logger.info("RemoteSync", "Desktop registered with relay for persistent pairing")` |
| `console.log("[RemoteSync] Peer joined via pairing")` | `logger.info("RemoteSync", "Peer joined via pairing")` |
| `console.warn("[RemoteSync] Failed to register with relay:", err)` | `logger.warn("RemoteSync", "Failed to register with relay: " + String(err))` |
| `console.warn("[RemoteSync] Failed to issue pairing token:", err)` | `logger.warn("RemoteSync", "Failed to issue pairing token: " + String(err))` |
| `console.warn("[RemoteSync] Failed to list paired devices:", err)` | `logger.warn("RemoteSync", "Failed to list paired devices: " + String(err))` |
| `console.warn("[RemoteSync] Failed to revoke pairing:", err)` | `logger.warn("RemoteSync", "Failed to revoke pairing: " + String(err))` |
| `console.warn("[RemoteSync] syncFullState failed:", err)` | `logger.warn("RemoteSync", "syncFullState failed: " + String(err))` |
| `console.log("[RemoteSync] PIN auth succeeded")` | `logger.info("RemoteSync", "PIN auth succeeded")` |
| `console.log("[RemoteSync] Pairing token issued to mobile")` | `logger.info("RemoteSync", "Pairing token issued to mobile")` |
| `console.warn("[RemoteSync] Failed to auto-issue pairing token:", err)` | `logger.warn("RemoteSync", "Failed to auto-issue pairing token: " + String(err))` |
| `console.warn("[RemoteSync] PIN auth failed...")` | `logger.warn("RemoteSync", "PIN auth failed - incorrect PIN")` |
| `console.warn("[RemoteSync] Auth handling error:", err)` | `logger.warn("RemoteSync", "Auth handling error: " + String(err))` |
| `console.warn("[RemoteSync] Connection handler error:", err)` | `logger.warn("RemoteSync", "Connection handler error: " + String(err))` |

General rule for `err` arguments: Use `String(err)` to convert to string since logger takes `(context, message)` not `(context, message, ...args)`.

**tree-store.ts (11 calls):**
| Pattern | Replacement |
|---------|-------------|
| `console.warn("[ATM] Could not create .aui directory:", dirErr)` | `logger.warn("TreeStore", "Could not create .aui directory: " + String(dirErr))` |
| `console.warn("[ATM] Failed to parse file:", filePath, e)` | `logger.warn("TreeStore", "Failed to parse file: " + filePath + " " + String(e))` |
| `console.log("[ATM] Loaded ${nodes.size} nodes...")` | `logger.info("TreeStore", \`Loaded ${nodes.size} nodes (${skillCount} skills, ${skillNameCache.size} cached names)\`)` |
| `console.warn("[ATM] Failed to save metadata during load:", saveErr)` | `logger.warn("TreeStore", "Failed to save metadata during load: " + String(saveErr))` |
| `console.warn("[ATM] Failed to load layouts:", layoutErr)` | `logger.warn("TreeStore", "Failed to load layouts: " + String(layoutErr))` |
| `console.error("[ATM] loadProject failed:", err)` | `logger.error("TreeStore", "loadProject failed: " + String(err))` |
| `console.error("[ATM] saveTreeMetadata failed:", err)` | `logger.error("TreeStore", "saveTreeMetadata failed: " + String(err))` |
| `console.warn("[ATM] Could not read skill file:...")` | `logger.warn("TreeStore", "Could not read skill file: " + node.sourcePath)` |
| `console.warn("[ATM] Could not read assigned skill file:...")` | `logger.warn("TreeStore", "Could not read assigned skill file: " + skillNode.sourcePath)` |
| `console.warn("[ATM] Failed to write skill file...")` | `logger.warn("TreeStore", "Failed to write skill file " + relativePath + ": " + String(err))` |
| `console.log("[RemoteSync] Sent full resync...")` | `logger.info("RemoteSync", "Sent full resync after reconnect")` |

**ui-store.ts (4 calls):**
| Pattern | Replacement |
|---------|-------------|
| `console.warn("[ATM] Failed to load remote config:", err)` | `logger.warn("UiStore", "Failed to load remote config: " + String(err))` |
| `console.warn("[ATM] Failed to save remote config:", err)` | `logger.warn("UiStore", "Failed to save remote config: " + String(err))` |
| `console.warn("[ATM] Failed to connect to relay:", err)` | `logger.warn("UiStore", "Failed to connect to relay: " + String(err))` |
| `console.warn("[ATM] Failed to connect to relay (persistent):", err)` | `logger.warn("UiStore", "Failed to connect to relay (persistent): " + String(err))` |

Remove any `[ATM]` or `[RemoteSync]` prefix from the message strings — the logger adds `[ATM]` automatically and the context parameter replaces the inline prefix.
  </action>
  <verify>
    <automated>grep -c "console\.\(log\|warn\|error\)" src/services/remote-sync.ts src/store/tree-store.ts src/store/ui-store.ts 2>/dev/null | grep -v ":0$" | wc -l | xargs -I{} test {} -eq 0 && echo "NO CONSOLE LEFT IN BATCH 1" && grep -l "import.*logger.*from" src/services/remote-sync.ts src/store/tree-store.ts src/store/ui-store.ts | wc -l | xargs -I{} test {} -eq 3 && echo "ALL IMPORTS OK"</automated>
  </verify>
  <acceptance_criteria>
    - src/services/remote-sync.ts contains 0 console.log/warn/error calls
    - src/store/tree-store.ts contains 0 console.log/warn/error calls
    - src/store/ui-store.ts contains 0 console.log/warn/error calls
    - All 3 files import `{ logger }` from `@/services/logger`
    - No `[ATM]` prefix in message strings (logger adds it automatically)
    - Error objects converted to string via `String(err)` pattern
  </acceptance_criteria>
  <done>50 console calls replaced with logger calls across remote-sync.ts, tree-store.ts, and ui-store.ts</done>
</task>

<task type="auto">
  <name>Task 2: Replace console calls in App.tsx, SettingsPanel.tsx, OrgNode.tsx</name>
  <files>src/App.tsx, src/components/settings/SettingsPanel.tsx, src/components/tree/OrgNode.tsx</files>
  <read_first>
    - src/App.tsx (lines 150-160 for console calls)
    - src/components/settings/SettingsPanel.tsx (lines 660-710 for console calls)
    - src/components/tree/OrgNode.tsx (line 84 for console call)
    - src/services/logger.ts
  </read_first>
  <action>
Add `import { logger } from "@/services/logger";` at the top of each file.

**App.tsx (2 calls):**
| Pattern | Replacement |
|---------|-------------|
| `console.log("[ATM] Loading project from:", targetPath)` | `logger.info("App", "Loading project from: " + targetPath)` |
| `console.log("[ATM] Platform:", navigator.userAgent)` | `logger.debug("App", "Platform: " + navigator.userAgent)` |

**SettingsPanel.tsx (2 calls):**
| Pattern | Replacement |
|---------|-------------|
| `console.warn("[Settings] Remote server error:", err)` | `logger.warn("Settings", "Remote server error: " + String(err))` |
| `console.warn("[Settings] Cloud relay error:", err)` | `logger.warn("Settings", "Cloud relay error: " + String(err))` |

**OrgNode.tsx (1 call):**
| Pattern | Replacement |
|---------|-------------|
| `console.warn(\`[ATM] Skill ID "${skillId}" not resolved...\`)` | `logger.warn("OrgNode", \`Skill ID "${skillId}" not resolved for group "${node.name}"\`)` |

After all replacements, run a project-wide search to confirm zero console.log/warn/error calls remain outside of logger.ts:
```bash
grep -rn "console\.\(log\|warn\|error\)" src/ --include="*.ts" --include="*.tsx" | grep -v "logger.ts" | grep -v "node_modules"
```
This should return 0 results.
  </action>
  <verify>
    <automated>grep -rn "console\.\(log\|warn\|error\)" src/ --include="*.ts" --include="*.tsx" | grep -v "logger.ts" | grep -v "node_modules" | wc -l | xargs -I{} test {} -eq 0 && echo "ZERO CONSOLE CALLS REMAINING" && pnpm build 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx contains 0 console.log/warn/error calls
    - src/components/settings/SettingsPanel.tsx contains 0 console.log/warn/error calls
    - src/components/tree/OrgNode.tsx contains 0 console.log/warn/error calls
    - All 3 files import `{ logger }` from `@/services/logger`
    - Zero console.log/warn/error calls exist in any .ts/.tsx file under src/ (except logger.ts)
    - `pnpm build` completes without type errors
  </acceptance_criteria>
  <done>All 55 console calls replaced with structured logger calls. Zero console.log/warn/error remaining in source. pnpm build passes.</done>
</task>

</tasks>

<verification>
- `pnpm build` completes without errors
- `grep -rn "console\.\(log\|warn\|error\)" src/ --include="*.ts" --include="*.tsx" | grep -v "logger.ts"` returns 0 results
- `grep -rn "import.*logger.*from" src/ --include="*.ts" --include="*.tsx" | grep -v "logger.ts"` returns 6 results (one per file)
</verification>

<success_criteria>
- Zero console.log/warn/error calls remain in source code (outside logger.ts)
- All 6 files import and use the logger facade
- Log messages use appropriate levels (info for normal operations, warn for recoverable errors, error for failures, debug for platform info)
- Each logger call includes a context string identifying the source module
- pnpm build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-04-SUMMARY.md`
</output>
